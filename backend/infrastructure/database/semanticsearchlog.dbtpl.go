package database

// Code generated by dbtpl. DO NOT EDIT.

import (
	"context"
	"time"

	"github.com/google/uuid"
)

// SemanticSearchLog represents a row from 'public.semantic_search_logs'.
type SemanticSearchLog struct {
	ID        uuid.UUID `json:"id"`         // id
	UserID    uuid.UUID `json:"user_id"`    // user_id
	CreatedAt time.Time `json:"created_at"` // created_at
	// xo fields
	_exists, _deleted bool
}

// Exists returns true when the [SemanticSearchLog] exists in the database.
func (ssl *SemanticSearchLog) Exists() bool {
	return ssl._exists
}

// Deleted returns true when the [SemanticSearchLog] has been marked for deletion
// from the database.
func (ssl *SemanticSearchLog) Deleted() bool {
	return ssl._deleted
}

// Insert inserts the [SemanticSearchLog] to the database.
func (ssl *SemanticSearchLog) Insert(ctx context.Context, db DB) error {
	switch {
	case ssl._exists: // already exists
		return logerror(&ErrInsertFailed{ErrAlreadyExists})
	case ssl._deleted: // deleted
		return logerror(&ErrInsertFailed{ErrMarkedForDeletion})
	}
	// insert (manual)
	const sqlstr = `INSERT INTO public.semantic_search_logs (` +
		`id, user_id, created_at` +
		`) VALUES (` +
		`$1, $2, $3` +
		`)`
	// run
	logf(sqlstr, ssl.ID, ssl.UserID, ssl.CreatedAt)
	if _, err := db.ExecContext(ctx, sqlstr, ssl.ID, ssl.UserID, ssl.CreatedAt); err != nil {
		return logerror(err)
	}
	// set exists
	ssl._exists = true
	return nil
}

// Update updates a [SemanticSearchLog] in the database.
func (ssl *SemanticSearchLog) Update(ctx context.Context, db DB) error {
	switch {
	case !ssl._exists: // doesn't exist
		return logerror(&ErrUpdateFailed{ErrDoesNotExist})
	case ssl._deleted: // deleted
		return logerror(&ErrUpdateFailed{ErrMarkedForDeletion})
	}
	// update with composite primary key
	const sqlstr = `UPDATE public.semantic_search_logs SET ` +
		`user_id = $1, created_at = $2 ` +
		`WHERE id = $3`
	// run
	logf(sqlstr, ssl.UserID, ssl.CreatedAt, ssl.ID)
	if _, err := db.ExecContext(ctx, sqlstr, ssl.UserID, ssl.CreatedAt, ssl.ID); err != nil {
		return logerror(err)
	}
	return nil
}

// Save saves the [SemanticSearchLog] to the database.
func (ssl *SemanticSearchLog) Save(ctx context.Context, db DB) error {
	if ssl.Exists() {
		return ssl.Update(ctx, db)
	}
	return ssl.Insert(ctx, db)
}

// Upsert performs an upsert for [SemanticSearchLog].
func (ssl *SemanticSearchLog) Upsert(ctx context.Context, db DB) error {
	switch {
	case ssl._deleted: // deleted
		return logerror(&ErrUpsertFailed{ErrMarkedForDeletion})
	}
	// upsert
	const sqlstr = `INSERT INTO public.semantic_search_logs (` +
		`id, user_id, created_at` +
		`) VALUES (` +
		`$1, $2, $3` +
		`)` +
		` ON CONFLICT (id) DO ` +
		`UPDATE SET ` +
		`user_id = EXCLUDED.user_id, created_at = EXCLUDED.created_at `
	// run
	logf(sqlstr, ssl.ID, ssl.UserID, ssl.CreatedAt)
	if _, err := db.ExecContext(ctx, sqlstr, ssl.ID, ssl.UserID, ssl.CreatedAt); err != nil {
		return logerror(err)
	}
	// set exists
	ssl._exists = true
	return nil
}

// Delete deletes the [SemanticSearchLog] from the database.
func (ssl *SemanticSearchLog) Delete(ctx context.Context, db DB) error {
	switch {
	case !ssl._exists: // doesn't exist
		return nil
	case ssl._deleted: // deleted
		return nil
	}
	// delete with single primary key
	const sqlstr = `DELETE FROM public.semantic_search_logs ` +
		`WHERE id = $1`
	// run
	logf(sqlstr, ssl.ID)
	if _, err := db.ExecContext(ctx, sqlstr, ssl.ID); err != nil {
		return logerror(err)
	}
	// set deleted
	ssl._deleted = true
	return nil
}

// SemanticSearchLogsByCreatedAt retrieves a row from 'public.semantic_search_logs' as a [SemanticSearchLog].
//
// Generated from index 'idx_semantic_search_logs_created_at'.
func SemanticSearchLogsByCreatedAt(ctx context.Context, db DB, createdAt time.Time) ([]*SemanticSearchLog, error) {
	// query
	const sqlstr = `SELECT ` +
		`id, user_id, created_at ` +
		`FROM public.semantic_search_logs ` +
		`WHERE created_at = $1`
	// run
	logf(sqlstr, createdAt)
	rows, err := db.QueryContext(ctx, sqlstr, createdAt)
	if err != nil {
		return nil, logerror(err)
	}
	defer rows.Close()
	// process
	var res []*SemanticSearchLog
	for rows.Next() {
		ssl := SemanticSearchLog{
			_exists: true,
		}
		// scan
		if err := rows.Scan(&ssl.ID, &ssl.UserID, &ssl.CreatedAt); err != nil {
			return nil, logerror(err)
		}
		res = append(res, &ssl)
	}
	if err := rows.Err(); err != nil {
		return nil, logerror(err)
	}
	return res, nil
}

// SemanticSearchLogsByUserID retrieves a row from 'public.semantic_search_logs' as a [SemanticSearchLog].
//
// Generated from index 'idx_semantic_search_logs_user_id'.
func SemanticSearchLogsByUserID(ctx context.Context, db DB, userID uuid.UUID) ([]*SemanticSearchLog, error) {
	// query
	const sqlstr = `SELECT ` +
		`id, user_id, created_at ` +
		`FROM public.semantic_search_logs ` +
		`WHERE user_id = $1`
	// run
	logf(sqlstr, userID)
	rows, err := db.QueryContext(ctx, sqlstr, userID)
	if err != nil {
		return nil, logerror(err)
	}
	defer rows.Close()
	// process
	var res []*SemanticSearchLog
	for rows.Next() {
		ssl := SemanticSearchLog{
			_exists: true,
		}
		// scan
		if err := rows.Scan(&ssl.ID, &ssl.UserID, &ssl.CreatedAt); err != nil {
			return nil, logerror(err)
		}
		res = append(res, &ssl)
	}
	if err := rows.Err(); err != nil {
		return nil, logerror(err)
	}
	return res, nil
}

// SemanticSearchLogByID retrieves a row from 'public.semantic_search_logs' as a [SemanticSearchLog].
//
// Generated from index 'semantic_search_logs_pkey'.
func SemanticSearchLogByID(ctx context.Context, db DB, id uuid.UUID) (*SemanticSearchLog, error) {
	// query
	const sqlstr = `SELECT ` +
		`id, user_id, created_at ` +
		`FROM public.semantic_search_logs ` +
		`WHERE id = $1`
	// run
	logf(sqlstr, id)
	ssl := SemanticSearchLog{
		_exists: true,
	}
	if err := db.QueryRowContext(ctx, sqlstr, id).Scan(&ssl.ID, &ssl.UserID, &ssl.CreatedAt); err != nil {
		return nil, logerror(err)
	}
	return &ssl, nil
}

// User returns the User associated with the [SemanticSearchLog]'s (UserID).
//
// Generated from foreign key 'semantic_search_logs_user_id_fkey'.
func (ssl *SemanticSearchLog) User(ctx context.Context, db DB) (*User, error) {
	return UserByID(ctx, db, ssl.UserID)
}
